/**
 * Represents the user information.
 */
export interface User {
  /** The user's id. */
  id: string;

  /** The user's first name. */
  firstName: string;

  /** The user's last name. */
  lastName: string;

  /** The user's username. */
  username: string;

  /** The user's email. */
  email: string;
}

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  closeIcon: { position: 'absolute', zIndex: 10, top: 6, right: 6 },
  image: { width: '100%', height: 230, borderRadius: 6 },
});